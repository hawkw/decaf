int Binky(int a, int[] b, int[] c)
{
  return b[c[0]];
}

void main()
{
  int[] c;
  int [][]d;

  d = NewArray(5, int[]);
  d[0] = NewArray(12, int);
  c = NewArray(10, int);
  c[0] = 4 + 5 * 3 / 4 % 2;
  d[0][c[0]] = 55;

  Print(c[0], " ", 2 * Binky(100, d[0], c));
}
