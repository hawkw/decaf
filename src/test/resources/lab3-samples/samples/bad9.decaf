double Pi(int a) { return 3.14159; }
void main()
{
  int a;
   string[] arr;
   a = "hello";
   Print(3.5, a, Pi(10));
   a = 10 + main;
   arr = NewArray(2, string);
   for (a = 0; a < arr.length(); a = a + 1)
     Print(arr[a], a.length());
}
